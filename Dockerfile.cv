FROM python:3.10-slim

WORKDIR /app

# Install system dependencies for OpenCV and dlib (cmake, build-essential)
RUN apt-get update && apt-get install -y \
    libglib2.0-0 \
    libsm6 \
    libxext6 \
    cmake \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

# Install python packages including face_recognition (this installs dlib)
RUN pip install --no-cache-dir opencv-python-headless mysql-connector-python requests face_recognition numpy Pillow flask

# Copy worker script
COPY cv_worker.py .

CMD ["python", "cv_worker.py"]
